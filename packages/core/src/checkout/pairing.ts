/** Flat bundle price for "any shirt + any trouser, one size" — Rs 5,200,
 *  regardless of which two products, matching the storefront-2 design's own
 *  business rule (not "sum minus a discount"). See PLACEHOLDER_DATA.md. */
export const PAIR_PRICE_CENTS = 520000;
